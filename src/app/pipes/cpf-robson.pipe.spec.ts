import { CpfRobsonPipe } from './cpf-robson.pipe';

describe('CpfRobsonPipe', () => {
  it('create an instance', () => {
    const pipe = new CpfRobsonPipe();
    expect(pipe).toBeTruthy();
  });
});
