import { CpfMarceloPipe } from './cpf-marcelo.pipe';

describe('CpfMarceloPipe', () => {
  it('create an instance', () => {
    const pipe = new CpfMarceloPipe();
    expect(pipe).toBeTruthy();
  });
  
});
