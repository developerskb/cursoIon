import { TelefoneMarceloPipe } from './telefone-marcelo.pipe';

describe('TelefoneMarceloPipe', () => {
  it('create an instance', () => {
    const pipe = new TelefoneMarceloPipe();
    expect(pipe).toBeTruthy();
  });
});
