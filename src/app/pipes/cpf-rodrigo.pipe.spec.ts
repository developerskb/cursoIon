import { CpfRodrigoPipe } from './cpf-rodrigo.pipe';

describe('CpfRodrigoPipe', () => {
  it('create an instance', () => {
    const pipe = new CpfRodrigoPipe();
    expect(pipe).toBeTruthy();
  });
});
